let myString = "javascript+es+super+cool";

// Convertir string en array eliminande el caracter +

arr = myString.split("+");

console.log(myString, arr);

// Guardar resultado en una variable

const myArray = arr;

console.log(myArray);

// Almacenar la longitud del array en una variable

let arrayLength = arr;

console.log(arrayLength.length);

// Almacenar el último elemento del array en una variable

let lastItem = arr.pop();

console.log(lastItem);

// Crear un nuevo array en el que se inviertan los elementos ordenados del primer array

myInverseArray = arr.reverse();

console.log(myInverseArray);
